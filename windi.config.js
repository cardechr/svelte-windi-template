import { defineConfig } from "windicss/helpers";

export default defineConfig({
   darkMode: 'class',
   theme: {
       extend: {
           fontFamily: {
               sans: ['Roboto', 'sans-serif']
           }
       }
   }
});